fun test()
    data : i8[10]
    for i:= until(data.len)
        data[i]! = i
    sum := 0
    for i:= range(0 20)
        sum += data[i]
    println(sum)

test()

## Expected
Panic: Array index 10 is out of bounds for the array length 10
